import React from 'react';
import {BrowserRouter} from 'react-router-dom';
import RoutesHands from './Routes';

function App() {
  return (
    <BrowserRouter>
        <RoutesHands />
    </BrowserRouter>
  );
}

export default App;
